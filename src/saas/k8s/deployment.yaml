# Combined Kubernetes Configuration

---
# Namespace
apiVersion: v1
kind: Namespace
metadata:
  name: fluid-calendar

---
# Service Account and RBAC
apiVersion: v1
kind: ServiceAccount
metadata:
  name: fluid-calendar-sa
  namespace: fluid-calendar
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: job-reader
  namespace: fluid-calendar
rules:
- apiGroups: ["batch"]
  resources: ["jobs"]
  verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: fluid-calendar-job-reader
  namespace: fluid-calendar
subjects:
- kind: ServiceAccount
  name: fluid-calendar-sa
  namespace: fluid-calendar
roleRef:
  kind: Role
  name: job-reader
  apiGroup: rbac.authorization.k8s.io

---
# Service
apiVersion: v1
kind: Service
metadata:
  name: fluid-calendar
  namespace: fluid-calendar
spec:
  selector:
    app: fluid-calendar
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP

---
# Redis StatefulSet for BullMQ
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis
  namespace: fluid-calendar
spec:
  serviceName: redis
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
          name: redis
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "200m"
            memory: "512Mi"
        volumeMounts:
        - name: redis-data
          mountPath: /data
        args: ["--appendonly", "yes"]
        livenessProbe:
          tcpSocket:
            port: redis
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          tcpSocket:
            port: redis
          initialDelaySeconds: 5
          periodSeconds: 10
  volumeClaimTemplates:
  - metadata:
      name: redis-data
    spec:
      accessModes: ["ReadWriteOnce"]
      storageClassName: do-block-storage
      resources:
        requests:
          storage: 1Gi

---
# Redis Service
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: fluid-calendar
spec:
  selector:
    app: redis
  ports:
  - port: 6379
    targetPort: 6379
    name: redis
  clusterIP: None

---
# Migration Job
apiVersion: batch/v1
kind: Job
metadata:
  name: db-migrate
  namespace: fluid-calendar
spec:
  backoffLimit: 4
  template:
    spec:
      imagePullSecrets:
        - name: do-registry-secret
      containers:
      - name: db-migrate
        image: registry.digitalocean.com/curatedletters/fluid-calendar-website:latest
        imagePullPolicy: Always
        command: ["npm"]
        args: ["run", "prisma:update"]
        env:
        - name: INFISICAL_TOKEN
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: INFISICAL_TOKEN
        - name: PROJECT_ID
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: PROJECT_ID
        - name: INFISICAL_SECRET_ENV
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: INFISICAL_SECRET_ENV
        - name: INFISICAL_API_URL
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: INFISICAL_API_URL
      restartPolicy: Never

---
# Main Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fluid-calendar
  namespace: fluid-calendar
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fluid-calendar
  template:
    metadata:
      labels:
        app: fluid-calendar
    spec:
      serviceAccountName: fluid-calendar-sa
      imagePullSecrets:
        - name: do-registry-secret
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - fluid-calendar
              topologyKey: "kubernetes.io/hostname"
      initContainers:
      - name: wait-for-migration
        image: bitnami/kubectl:latest
        command: ['sh', '-c', 'until kubectl get job db-migrate -n fluid-calendar -o jsonpath="{.status.succeeded}" | grep 1; do echo waiting for migration; sleep 2; done;']
      containers:
      - name: fluid-calendar
        image: registry.digitalocean.com/curatedletters/fluid-calendar-website:latest
        imagePullPolicy: Always
        env:
        - name: INFISICAL_TOKEN
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: INFISICAL_TOKEN
        - name: PROJECT_ID
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: PROJECT_ID
        - name: INFISICAL_SECRET_ENV
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: INFISICAL_SECRET_ENV
        - name: INFISICAL_API_URL
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: INFISICAL_API_URL
        - name: REDIS_HOST
          value: "redis"
        - name: REDIS_PORT
          value: "6379"
        ports:
        - containerPort: 3000
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "1024Mi"
        livenessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5

---
# Worker Deployment for BullMQ
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fluid-calendar-worker
  namespace: fluid-calendar
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fluid-calendar-worker
  template:
    metadata:
      labels:
        app: fluid-calendar-worker
    spec:
      serviceAccountName: fluid-calendar-sa
      imagePullSecrets:
        - name: do-registry-secret
      initContainers:
      - name: wait-for-migration
        image: bitnami/kubectl:latest
        command: ['sh', '-c', 'until kubectl get job db-migrate -n fluid-calendar -o jsonpath="{.status.succeeded}" | grep 1; do echo waiting for migration; sleep 2; done;']
      containers:
      - name: worker
        image: registry.digitalocean.com/curatedletters/fluid-calendar-website:latest
        imagePullPolicy: Always
        command: ["npm"]
        args: ["run", "start:worker:prod"]
        env:
        - name: INFISICAL_TOKEN
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: INFISICAL_TOKEN
        - name: PROJECT_ID
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: PROJECT_ID
        - name: INFISICAL_SECRET_ENV
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: INFISICAL_SECRET_ENV
        - name: INFISICAL_API_URL
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: INFISICAL_API_URL
        - name: REDIS_HOST
          value: "redis"
        - name: REDIS_PORT
          value: "6379"
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "300m"
            memory: "512Mi"
        livenessProbe:
          exec:
            command:
            - node
            - -e
            - "process.exit(0)"
          initialDelaySeconds: 30
          periodSeconds: 30

---
# HorizontalPodAutoscaler
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: fluid-calendar-hpa
  namespace: fluid-calendar
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: fluid-calendar
  minReplicas: 1
  maxReplicas: 3
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 80

---
# Ingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fluid-calendar
  namespace: fluid-calendar
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - fluidcalendar.com
    secretName: fluid-calendar-tls
  rules:
  - host: fluidcalendar.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: fluid-calendar
            port:
              number: 80 